[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — alle Routen liefern index.html (für späteres Routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
